module demoApp {

    export class HomeController {

        /** @ngInject */
        constructor(
        ) {
        }

    }

    angular.module('demoApp').controller('HomeController', HomeController);
}
